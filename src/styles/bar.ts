import { colors } from "./colors"

export const bar = {
    
    container: {
        width: '100%',
        backgroundColor: colors.secondColor,
        marginBottom: -3,
    },
    barText: {
        marginTop: 25,
        textAlign: 'center',
        color: colors.secondTextColor,
        fontSize: 22,
        fontWeight: '500',
        padding: 8,
    },
}