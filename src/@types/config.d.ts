export declare global {
    namespace Config {
        type Categories = {
            _id: string,
            value: string,
            icon: string,
            lastChange: date
        }
    }
}
